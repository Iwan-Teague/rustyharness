[package]
name = "gate-outcome"
description = "The ONE gate-layer outcome type: GateOutcome { Passed(Witness), Failed, Indeterminate }. Std-only, pure."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true

[features]
default = []
# Wire encoding for the §6 child protocol (UNIFIED gate-outcome v0.3 §6).
json = ["dep:serde", "dep:serde_json"]

[dependencies]
serde = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }

[lints]
workspace = true
